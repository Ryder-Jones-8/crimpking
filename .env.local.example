# Supabase (leave blank to use local mock-data/localStorage fallback mode)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI (server-only, optional — leave blank to use local heuristic summarization)
OPENAI_API_KEY=
